// DXGI Desktop Duplication frame source (reelform-dxgi.exe), fallback for
// Windows builds < 19041 (§5.4). Displays and regions only; no window capture.
// UNVERIFIED: written without a Windows toolchain; not compiled on the authoring machine.
#pragma once

#include "frame_source.hpp"
